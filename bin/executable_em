#!/usr/bin/env -S fish -li


function em -d "Open in Emacs"
    emacsclient -c -n $argv || 
    start_em_daemon $argv
end 

function start_em_daemon -d "Start Deamon and open Files"
    emacs --bg-daemon &&
    em $argv
end


em $argv
