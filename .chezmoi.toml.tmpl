{{ $email := promptString "email" -}}

[data]
email = {{ $email | quote }}
zotraPath = '--git-dir={{ .chezmoi.homeDir }}/.config/zotra-cli/.git'
zotraPathSub = '--git-dir={{ .chezmoi.homeDir }}/.config/zotra-cli/module/translation-server/.git'


[git]
    autoAdd = false
    autoCommit = false
